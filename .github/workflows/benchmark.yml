name: Benchmark

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main", "release/**"]
    types: ["opened", "reopened", "synchronize"]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  benchmarks:
    name: Compare Benchmarks
    runs-on: macos-26
    timeout-minutes: 20
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Get Swift Version
        run: swift --version
      - name: Select Comparison Revision
        id: comparison
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          BEFORE_SHA: ${{ github.event.before }}
          EVENT_NAME: ${{ github.event_name }}
          REF_NAME: ${{ github.ref_name }}
        run: |
          baseline_sha=$(swift Scripts/SelectBenchmarkBaseline.swift "$EVENT_NAME" "$BASE_SHA" "$BEFORE_SHA" "$REF_NAME")
          echo "baseline_sha=$baseline_sha" >> "$GITHUB_OUTPUT"
      - name: Compare Benchmarks
        env:
          BASELINE_SHA: ${{ steps.comparison.outputs.baseline_sha }}
        run: swift Scripts/RunBenchmarks.swift "$BASELINE_SHA" >> "$GITHUB_STEP_SUMMARY"
